<!DOCTYPE html>
<html>
  <head>
    <title>RailsRuby3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
      
   <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css' %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  </head>
  <nav id="nav">
    <ul class="container">
      <li><span class="nav-link px-2 link-secondary"><%= link_to "HomePage", home_path%></span></li>

      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <li><span class="nav-link px-2 link-secondary"><%= link_to "Admin", user_root_path%></span></li>
          <li><span class="nav-link px-2 link-secondary"><%= link_to "Create Post", new_admin_post_path%></span></li>
        <% else %>
          <li><span class="nav-link px-2 link-secondary"><%= link_to "Profile", user_root_path%></span></li>
        <% end %>
        <li><span class="nav-link px-2 link-secondary"><%= link_to "Log out", destroy_session_path(:user), :method => :delete %></span></li>
      <% else %>
        <li><span class="nav-link px-2 link-secondary"><%= link_to "Log In", new_user_session_path%></span></li>
      <% end %>

      <li><span class="nav-link px-2 link-secondary"><%= link_to "About Us", about_path%></span></li>
    </ul>
  </nav>




  </header>
  </div>
  <div class ="container">
  <body>
    <%= yield %>
  </body>
  </div>
  <footer>
    <!-- Contact -->
    <article id="contact" class="wrapper style4">
      <div class="container medium">
        <header>
          <h2>Have me make stuff for you.</h2>
          <p>Just do it</p>
        </header>
        <div class="row">
          <div class="col-12">
            <%= form_for Contact.new do |f| %>
              <div class="row">
                <div class="col-6 col-12-small">
                  Name<br>
                  <%= f.text_field(:name) %>
                </div>
                <div class="col-6 col-12-small">
                  Email<br>
                  <%= f.text_field(:email) %>
                </div>
                <div class="col-12">
                  Subject<br>
                  <%= f.text_field(:subject) %>
                </div>
                <div class="col-12">
                  Message<br>
                  <%= f.text_field(:message) %>
                </div>
                <div class="col-12">
                  <ul class="actions">
                    <%= f.submit "Send Message" %>
                    <li><input type="reset" value="Clear Form" class="alt" /></li>
                  </ul>
                </div>
              </div>
            <% end %>
          </div>
          <div class="col-12">
            <hr />
            <h3>Find me on ...</h3>
            <ul class="social">
              <li><a href="https://github.com/Kalsash" class="icon brands fa-github"><span class="label" >Github</span></a></li>
            </ul>
            <hr />
          </div>
        </div>
        <footer>
          <ul id="copyright">
            <li>&copy; Coding Blogs</li><li>Git: <a href="https://github.com/Kalsash/CodingBlogs">repository</a></li>
          </ul>
        </footer>
      </div>
    </article>
  </footer>
</html>
